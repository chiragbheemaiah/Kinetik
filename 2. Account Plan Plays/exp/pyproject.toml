[project]
name = "account-plan-plays"
version = "0.1.0"
description = "Extract and map use cases from account plan PowerPoints"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.0.0",
    "pydantic>=2.0.0",
    "python-pptx>=1.0.0",
    "lilypad>=0.1.0",
    "langsmith>=0.1.0",
]

[tool.uv]
dev-dependencies = []
